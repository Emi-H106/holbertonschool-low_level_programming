#include <stdio.h>
#include "main.h"

/**
 * _abs: Display the absolute value of an integer
 * 
 * Return: n or -n
 */

int _abs(int)
{
	int n;
	if (n < 0)
	{
		return -n;
	}
	else
	{
		return n;
	}
}
